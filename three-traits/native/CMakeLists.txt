cmake_minimum_required(VERSION 3.24)
project(BiggieTraitMechanics VERSION 1.6.1 LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>DLL")
set(BUILD_TESTS OFF CACHE BOOL "" FORCE)
set(ENABLE_SKYRIM_SE OFF CACHE BOOL "" FORCE)
set(ENABLE_SKYRIM_AE ON CACHE BOOL "" FORCE)
set(ENABLE_SKYRIM_VR OFF CACHE BOOL "" FORCE)
add_subdirectory("${COMMONLIB_ROOT}" CommonLibSSE)
find_package(minhook CONFIG REQUIRED)
add_library(BiggieTraitMechanics SHARED src/main.cpp src/Rules.h src/Skald.h src/SkaldRuntime.h src/SkaldDiagnostics.h src/SkaldDeferred.h src/EchoDiagnostics.h src/IronLungs.h src/IronLungsRuntime.h src/IronLungsGrant.h src/IronLungsImpact.h src/Overexertion.h)
target_link_libraries(BiggieTraitMechanics PRIVATE CommonLibSSE::CommonLibSSE minhook::minhook)
target_compile_definitions(BiggieTraitMechanics PRIVATE NOMINMAX)
add_executable(rules_tests tests/rules_tests.cpp)
target_include_directories(rules_tests PRIVATE src)
if(MSVC)
 target_compile_options(rules_tests PRIVATE /UNDEBUG)
endif()
enable_testing()
add_test(NAME traits_rules COMMAND rules_tests)
add_executable(lab_tests tests/lab_tests.cpp)
target_include_directories(lab_tests PRIVATE src)
if(MSVC)
 target_compile_options(lab_tests PRIVATE /UNDEBUG)
endif()
add_test(NAME lab_visit_rules COMMAND lab_tests)
add_executable(skald_tests tests/skald_tests.cpp)
target_include_directories(skald_tests PRIVATE src)
if(MSVC)
 target_compile_options(skald_tests PRIVATE /UNDEBUG)
endif()
add_test(NAME skald_recovery_and_echo COMMAND skald_tests)
add_executable(iron_lungs_tests tests/iron_lungs_tests.cpp)
target_include_directories(iron_lungs_tests PRIVATE src)
if(MSVC)
 target_compile_options(iron_lungs_tests PRIVATE /UNDEBUG)
endif()
add_test(NAME iron_lungs_cost_threshold_and_snapshot COMMAND iron_lungs_tests)
add_executable(overexertion_tests tests/overexertion_tests.cpp)
target_include_directories(overexertion_tests PRIVATE src)
if(MSVC)
 target_compile_options(overexertion_tests PRIVATE /UNDEBUG)
endif()
add_test(NAME iron_lungs_overexertion COMMAND overexertion_tests)
add_executable(skald_deferred_tests tests/skald_deferred_tests.cpp)
target_include_directories(skald_deferred_tests PRIVATE src)
if(MSVC)
 target_compile_options(skald_deferred_tests PRIVATE /UNDEBUG)
endif()
add_test(NAME skald_deferred_release_order COMMAND skald_deferred_tests)
add_executable(iron_lungs_impact_tests tests/iron_lungs_impact_tests.cpp)
target_include_directories(iron_lungs_impact_tests PRIVATE src)
if(MSVC)
 target_compile_options(iron_lungs_impact_tests PRIVATE /UNDEBUG)
endif()
add_test(NAME iron_lungs_impact_isolation COMMAND iron_lungs_impact_tests)
target_sources(BiggieTraitMechanics PRIVATE src/RunicOverdrive.h src/RunicOverdriveRuntime.h)
add_executable(runic_tests tests/runic_tests.cpp)
target_include_directories(runic_tests PRIVATE src)
if(MSVC)
 target_compile_options(runic_tests PRIVATE /UNDEBUG)
endif()
add_test(NAME runic_overdrive_scaling_and_hit_isolation COMMAND runic_tests)
target_sources(BiggieTraitMechanics PRIVATE src/ArcaneDynamo.h src/ArcaneDynamoRuntime.h)
add_executable(dynamo_tests tests/dynamo_tests.cpp)
target_include_directories(dynamo_tests PRIVATE src)
if(MSVC)
 target_compile_options(dynamo_tests PRIVATE /UNDEBUG)
endif()
add_test(NAME arcane_dynamo_cost_damage_and_isolation COMMAND dynamo_tests)

add_executable(dynamo_charge_tests tests/dynamo_charge_tests.cpp)
target_include_directories(dynamo_charge_tests PRIVATE src)
if(MSVC)
 target_compile_options(dynamo_charge_tests PRIVATE /UNDEBUG)
endif()
add_test(NAME arcane_dynamo_charge_order_and_cost_isolation COMMAND dynamo_charge_tests)
